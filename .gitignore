# ⚠️ 注意:本仓库刻意追踪 node_modules/(Windows-only,团队内免 npm install)
# 任何跑 `npm install` 后,请勿把改动 commit 进去(用 `git checkout -- node_modules/` 还原)
node_modules/
# ---------- Node / Vite ----------
# (dist/ 不忽略 — 部署走 git pull,build 产物直接入仓)
.vite/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
.eggs/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ---------- 临时文件 / 用户上传 ----------
*.mp4
*.mov
*.wav
*.mp3
*.m4a
*.flac
*.webm
uploads/
tmp/

# ---------- 环境变量 / 密钥 ----------
.env
.env.local
.env.*.local

# ---------- IDE / 编辑器 ----------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---------- OS ----------
desktop.ini
